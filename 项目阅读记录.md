# 灵石解决方案工作台：项目阅读记录

> 这是一份持续更新的项目认知档案。它不追求解释每一行代码，而是帮助我建立项目地图、理解完整运行流程、掌握关键技术点，并能够配合 AI 完成后续开发。

## 1. 阅读目标

### 我的当前基础

- 熟悉本项目的业务需求，需求文档由我编写。
- 会一些 Python，但目前只能直接读懂少量代码。
- 已经通过 `启动 Demo.bat` 启动并使用过项目。

### 希望达到的状态

- 能说清项目的整体结构和主要技术框架。
- 知道每个重要目录、文件和模块负责什么。
- 能说清项目从启动到完成一次核心业务操作的完整流程。
- 理解项目中真正重要的技术点，不强求逐行理解全部代码。
- 能判断一个功能修改会影响哪些部分。
- 能清楚地向 AI 描述开发任务，并检查 AI 的修改是否合理、完整。
- 在掌握现状的基础上，继续深化和优化项目功能。

## 2. 阅读方法

项目阅读按“先地图、后路线、再深入”的方式进行：

1. **先看全景**：辨认目录、技术栈、入口和主要模块。
2. **再走流程**：沿真实业务操作追踪代码，而不是孤立地读文件。
3. **最后拆核心**：集中理解决定功能行为的关键代码、配置和数据。

每个模块控制在约 30 分钟，基本节奏为：

- 5 分钟：明确本模块要回答的问题。
- 15 分钟：结合真实文件和代码讲解。
- 5 分钟：整理结构图、流程图或文件关系。
- 5 分钟：复述、提问与纠偏。

阅读过程中，对文件采用三级标记：

- **A级：必须理解**——入口、核心业务流程、重要数据与配置。
- **B级：知道职责**——需要会定位，但不必掌握全部实现细节。
- **C级：暂时忽略**——缓存、生成文件、第三方内容或非关键辅助文件。

## 3. 学习模块与进度

| 模块 | 主题 | 主要成果 | 状态 |
|---|---|---|---|
| 1 | 项目全景地图 | 目录树、技术栈、重点文件分级 | 进行中 |
| 2 | 项目启动过程 | `启动 Demo.bat` 到程序可用的启动链 | 未开始 |
| 3 | 完整业务主流程 | 一次核心业务操作的端到端调用链 | 未开始 |
| 4 | 核心框架与代码组织 | 界面、业务逻辑、数据等部分的关系 | 未开始 |
| 5 | 核心业务能力 | 关键业务规则及其代码落点 | 未开始 |
| 6 | 数据、配置与状态 | 数据库、配置文件、运行数据的关系 | 未开始 |
| 7 | 界面与后端连接 | 页面操作如何触发 Python 处理 | 未开始 |
| 8 | 异常、日志与排查 | 日志位置、报错路径、基本排障方法 | 未开始 |
| 9 | 一次真实的小修改 | 分析影响范围、修改、验证与复盘 | 未开始 |
| 10 | 深化与优化路线 | 架构问题、功能机会和开发优先级 | 未开始 |

状态统一使用：`未开始`、`进行中`、`已完成`、`需要回顾`。

## 4. 首轮项目快照

> 以下结论已经通过启动脚本、代码导入关系、配置文件和目录引用初步验证。

### 根目录

| 路径 | 当前判断 | 阅读级别 | 验证状态 |
|---|---|---:|---|
| `安全管家Demo需求文档.md` | 业务需求基线，描述目标、流程、功能、字段和验收场景 | A | 已看结构 |
| `安全管家Demo_阶段审核报告_v1.md` | 对当前 Demo 与需求、真实业务资料之间差异的阶段审核 | A | 已看结构和结论 |
| `启动 Demo.bat` | Windows 启动入口；检查端口后运行 `workbench/app.py` | A | 已确认 |
| `workbench/` | 当前版本的完整程序主体 | A | 已确认 |
| `资料输入区/` | 原始业务资料及此前在线填报系统；当前运行代码未直接引用 | A/B | 已初步确认 |
| `.workbuddy/` | 本地工具环境的辅助目录；不是当前业务源码主体 | C | 已初步确认 |

### `workbench/` 中已观察到的内容

| 路径 | 根据命名作出的初步判断 | 阅读级别 | 验证状态 |
|---|---|---:|---|
| `app.py` | Flask 应用入口与流程调度中心，包含 25 个页面/操作路由 | A | 已看骨架 |
| `core/` | 数据、提取、校验、计价、生成、打包等核心能力 | A | 已看接口骨架 |
| `templates/` | 10 个由后端填入数据的 HTML 页面模板 | A/B | 已看页面关系 |
| `static/` | 页面共用样式和浏览器端自动保存等交互 | B | 已看骨架 |
| `config/` | 应用、分部、产品、44 个字段、价格和 5 类生成物的定义 | A | 已看结构 |
| `data/` | SQLite 数据库、运行日志、生成结果和 ZIP 导出文件 | A/B | 已确认 |
| `requirements.txt` | Python 外部依赖清单，目前只有 Flask | B | 已确认 |
| `seed_demo.py` | 通过应用接口准备演示数据 | B | 已看接口骨架 |
| `selftest.py` | 对主要业务路径进行自动自检 | B | 已看接口骨架 |
| `samples/` | 完整与缺失两类文字稿样例 | B | 已确认 |
| `__pycache__/`、`*.pyc` | Python 自动生成的缓存 | C | 可忽略 |
| `*.db-wal`、`*.db-shm` | SQLite 运行时辅助文件 | C | 暂时忽略 |

## 5. 项目结构地图

当前项目是一个单体、本地运行的 Flask Web 应用。这里的“单体”是指页面入口、业务流程和数据访问都在同一个 Python 程序中，不需要分别启动前端和后端服务。

```text
灵石解决方案工作台
├── 需求与审核
│   ├── 安全管家Demo需求文档.md
│   └── 安全管家Demo_阶段审核报告_v1.md
├── 业务输入与历史参考
│   └── 资料输入区/
├── Windows 启动入口
│   └── 启动 Demo.bat
└── 当前程序主体 workbench/
    ├── app.py                  Flask 入口、页面地址与流程调度
    ├── core/                   可复用的后台能力
    │   ├── paths.py            统一管理目录路径
    │   ├── config.py           读取 JSON 配置
    │   ├── db.py               SQLite 建表与底层读写
    │   ├── repo.py             项目、字段、运行记录的数据操作
    │   ├── extract.py          从文字稿提取结构化字段
    │   ├── validate.py         完整性与业务规则校验
    │   ├── pricing.py          费用估算
    │   ├── generate.py         用模板生成成果文件
    │   └── packaging.py        汇总成果并打包 ZIP
    ├── config/                 应用和产品的可调整规则
    ├── templates/              用户看到的 HTML 页面骨架
    ├── static/                 页面样式与少量浏览器交互
    ├── data/                   运行数据、生成物与导出包
    ├── samples/                演示文字稿
    ├── seed_demo.py            演示数据初始化
    ├── selftest.py             自动自检
    └── requirements.txt        外部 Python 依赖
```

### 总体协作关系

```text
浏览器页面
    ↕
app.py（接收操作、决定下一步、选择页面）
    ↓
core/（执行数据、提取、校验、计价、生成、打包）
    ↙                         ↘
config/（规则与模板）          data/（数据库与生成文件）
```

可以把它理解为：`app.py` 是总调度台，`core/` 是专业部门，`config/` 是规则手册，`data/` 是档案室和成品仓库，`templates/` 是展示给用户的页面外壳。

### 技术栈

| 技术/框架 | 在项目中的用途 | 需要掌握到什么程度 |
|---|---|---|
| Python 3 | 整个后端、业务逻辑、数据操作和文件生成 | 能读懂主流程、函数调用和常见数据结构 |
| Flask | 本地 Web 服务、页面地址、请求接收和结果返回 | 理解“路由 → 函数 → 页面/跳转”即可 |
| Jinja2 | 把数据填入 HTML 页面和 Markdown 成果模板 | 会识别变量、循环、条件和模板入口 |
| HTML/CSS | 页面结构和视觉样式 | 能定位页面与样式，不要求系统学习前端 |
| 原生 JavaScript | 信息核对页自动保存、筛选、表格增删等交互 | 能判断何时由浏览器直接发请求即可 |
| SQLite | 保存项目、文字稿、字段、校验、生成和导出记录 | 理解表的用途及数据如何流转 |
| JSON 配置 | 定义应用参数、部门、产品、字段、价格和生成物 | 这是业务配置化的核心，需要重点掌握 |
| Markdown/Jinja 模板 | 生成踏勘结果、价值卡片等成果内容 | 理解模板如何引用经过确认的数据 |

## 6. 启动链

> 模块 2 完成后填写。

```text
双击“启动 Demo.bat”
→ 待补充
→ 待补充
→ 项目可访问
```

需要回答：

- `.bat` 脚本具体执行了哪些命令？
- Python 程序从哪个文件开始运行？
- 使用了哪些配置和环境？
- 是否启动了多个进程或服务？
- 项目的访问地址和端口在哪里确定？

## 7. 核心业务流程

> 模块 3 和模块 5 持续填写。每条流程都要能从业务名称追踪到代码与数据。

### 主流程 1：待选择

```text
用户操作
→ 页面或接口
→ 后端入口
→ 业务处理
→ 数据读写或内容生成
→ 返回结果
```

| 流程步骤 | 对应文件 | 关键函数/对象 | 输入 | 输出 |
|---|---|---|---|---|
| 待补充 | 待补充 | 待补充 | 待补充 | 待补充 |

## 8. 文件职责清单

> 不记录缓存和重复生成文件。重点记录以后开发时需要主动查找的文件。

| 文件 | 一句话职责 | 谁会调用它 | 它会调用谁 | 阅读级别 | 掌握状态 |
|---|---|---|---|---:|---|
| `启动 Demo.bat` | 定位程序目录、检查 8770 端口并运行 Python 入口 | 用户 | `workbench/app.py` | A | 已读骨架 |
| `workbench/app.py` | 定义 Flask 应用、25 个路由和整体操作流程 | 启动脚本、测试脚本 | `core/`、配置、页面模板 | A | 已读骨架 |
| `workbench/core/paths.py` | 统一计算配置、模板、数据等目录位置 | 多个核心模块 | 本地文件系统 | B | 已读 |
| `workbench/core/config.py` | 加载并缓存各类 JSON 配置 | `app.py` 和多个核心模块 | `config/` | A | 已读骨架 |
| `workbench/core/db.py` | 定义 7 张数据表并提供基础 SQL 操作 | `repo.py` | SQLite 数据库 | A | 已读骨架 |
| `workbench/core/repo.py` | 封装项目、产品、字段、运行记录的增删改查 | `app.py` 和业务模块 | `db.py` | A | 已看接口 |
| `workbench/core/extract.py` | 使用规则引擎或可选大模型从文字稿提取字段 | `app.py` | 字段配置、外部 LLM（默认关闭） | A | 已看接口 |
| `workbench/core/validate.py` | 检查必填项、字段格式和业务规则 | `app.py`、生成流程 | 字段数据和配置 | A | 已看接口 |
| `workbench/core/pricing.py` | 根据价格配置和确认数量形成费用估算 | 预览、生成和打包模块 | 字段数据、价格配置 | A | 已看接口 |
| `workbench/core/generate.py` | 组装上下文并通过 Jinja 模板生成 MD/HTML/CSV | `app.py` | 配置、数据库、成果模板 | A | 已看接口 |
| `workbench/core/packaging.py` | 将选定成果按目录规则打成 ZIP | `app.py` | 生成记录与输出文件 | B | 已看接口 |
| `workbench/templates/*.html` | 构成登录、项目、调研、核对、预览、结果等页面 | `app.py` | 页面数据、静态资源 | B | 已看关系 |
| `workbench/static/app.js` | 信息核对页的自动保存、筛选和表格交互 | HTML 页面 | 字段保存接口 | B | 已看接口 |
| `workbench/static/app.css` | 整个应用的页面样式 | HTML 页面 | 无 | C/B | 未细读 |
| `workbench/config/**/*.json` | 将产品字段、价格、输出等业务规则从代码中分离 | 配置模块 | 无 | A | 已看结构 |
| `workbench/config/**/templates/*.j2` | 定义 5 类成果文件的正文结构 | 生成模块 | 已确认字段和估算结果 | A/B | 未细读 |
| `workbench/data/workbench.db` | 保存用户操作形成的主要业务数据 | `db.py`、`repo.py` | 无 | A | 已确认用途 |

## 9. 关键技术点

> 每个技术点都结合项目中的真实用法理解，不单独背概念。

| 技术点 | 通俗解释 | 项目中的位置 | 为什么重要 | 掌握状态 |
|---|---|---|---|---|
| Python 模块与导入 | `app.py` 通过 `from core import ...` 使用其他文件提供的能力 | `app.py`、`core/` | 帮助理解文件间调用 | 已建立初步认识 |
| Flask 路由 | 把一个网址和一个 Python 函数绑定起来 | `app.py` 中的 `@app.route` | 是页面操作进入后端的入口 | 已识别，待深入 |
| Jinja 模板 | 后端把数据填入带占位符的页面或文档模板 | `templates/`、产品模板 | 串联数据与最终展示/成果 | 已识别，待深入 |
| 数据持久化 | 把项目状态写入 SQLite，使关闭程序后仍能保留 | `db.py`、`repo.py`、`data/` | 决定项目如何保存和恢复 | 已识别，待深入 |
| 配置驱动 | 字段、价格和生成物主要由 JSON 定义 | `config/` | 新产品和业务调整不必全部硬编码 | 已识别，待深入 |
| 分层职责 | 调度、业务能力、规则、页面和数据分别放置 | `app.py` 与各目录 | 帮助定位修改范围 | 已建立初步认识 |

## 10. 数据与配置

| 数据或配置 | 存放位置 | 作用 | 谁读取/修改 | 修改风险 |
|---|---|---|---|---|
| 待补充 | 待补充 | 待补充 | 待补充 | 待补充 |

需要重点区分：

- 固定配置与用户产生的数据。
- 数据库内容与文件形式的生成结果。
- 源代码、模板、运行日志和临时文件。
- 可以安全修改的内容与修改前必须备份的内容。

## 11. 问题与待验证判断

| 日期 | 问题或判断 | 当前答案 | 如何验证 | 状态 |
|---|---|---|---|---|
| 2026-09-07 | `workbench/app.py` 是否为唯一主入口？ | 它是正常使用 Demo 时的应用入口；`seed_demo.py` 和 `selftest.py` 是辅助脚本，也会导入它 | 已核对启动脚本、入口和导入关系 | 已验证 |
| 2026-09-07 | `资料输入区/此前制作的在线填报系统` 与当前系统是什么关系？ | 它是业务输入和历史参考，当前 `workbench/` 运行代码未直接读取；阶段审核报告指出两者字段模型存在明显差异 | 已搜索代码引用并查看审核报告 | 初步验证 |
| 2026-09-07 | 当前项目是否真正调用大模型？ | 默认不调用；`app.json` 选择离线规则引擎，LLM 提取通道存在但处于关闭状态 | 已核对应用配置和提取模块接口 | 已验证 |

## 12. 修改影响地图

> 模块 9 开始填写，用于以后向 AI 下达开发任务和审查修改。

| 想修改的内容 | 通常涉及的文件 | 需要同步检查 | 验证方式 |
|---|---|---|---|
| 页面展示 | 待补充 | 待补充 | 待补充 |
| 业务规则 | 待补充 | 待补充 | 待补充 |
| 数据字段 | 待补充 | 待补充 | 待补充 |
| 生成物模板 | 待补充 | 待补充 | 待补充 |

## 13. 优化与开发候选项

> 这里先记录观察，不急于立即修改。完成现状理解后再排序。

| 候选项 | 业务价值 | 技术原因 | 影响范围 | 优先级 | 是否已决定 |
|---|---|---|---|---|---|
| 让当前 44 字段模型与真实踏勘资料对齐 | 避免 Demo 与实际收资流程脱节 | 阶段审核报告指出真实旧系统有 84 个收资项，与当前字段模型重合很少 | 字段配置、提取、页面、生成模板、测试 | 高（待业务核实） | 否 |
| 替换演示价格并建立真实受控价格来源 | 避免将假设价格用于对客或报价 | 阶段审核报告明确指出当前价格为演示假设 | 价格配置、估算、生成物 | 高（待数据输入） | 否 |

## 14. 阅读日志

### 2026-09-07：阅读准备

**本次完成**

- 对齐了阅读目标：掌握结构、完整流程和关键技术点，不追求逐行理解。
- 确认项目当前可以通过 `启动 Demo.bat` 正常启动。
- 确认采用讲解与提问结合的方式，每个模块约 30 分钟。
- 创建本阅读记录，并登记首轮目录观察结果。

**当前形成的认识**

- 我对业务本身较熟悉，后续应把代码概念主动映射到需求文档中的业务概念。
- 当前程序主体很可能位于 `workbench/`，但需要通过启动脚本验证。
- 首个正式模块应从项目全景和文件分级开始。

**下次开始位置**

- 模块 1：项目全景地图。
- 先阅读需求文档的结构、启动脚本、依赖清单和主要代码目录。
- 输出经过验证的目录树、技术栈和第一版文件分级。

### 2026-09-07：模块 1——项目全景地图（讲解与确认阶段）

**本次查看范围**

- 根目录、需求与审核文档的标题结构。
- Windows 启动脚本和应用配置。
- 13 个 Python 文件的导入、类和函数骨架。
- 10 个 HTML 页面、共用 JavaScript 与样式文件。
- 产品、字段、价格、生成物配置及成果模板目录。
- SQLite 数据表结构及运行数据目录。

**已经确认**

- 当前程序主体完全位于 `workbench/`。
- 项目是本地运行的 Flask 单体 Web 应用，正常使用时只启动一个 Python 程序。
- Python 代码共约 3263 行，规模适合按完整业务流程阅读。
- `app.py` 是流程调度中心，`core/` 按专业能力拆分。
- 项目通过 JSON 配置定义 7 个字段分组、44 个字段（其中 18 个必填）和 5 类生成物。
- 页面由 Jinja 模板在服务端生成，浏览器端只有少量原生 JavaScript 交互。
- 数据由 SQLite 保存，文档成果写入 `data/outputs/`，ZIP 写入 `data/exports/`。
- 默认“AI 提取”实际使用离线规则引擎；大模型通道已预留但未启用。

**需要通过问答确认**

- 是否能区分 `app.py`、`core/`、`config/`、`templates/` 和 `data/` 的角色。
- 是否理解“配置驱动”对后续业务调整的意义。

## 15. 阶段复盘

### 我现在能说清楚什么？

- 项目不是多个服务拼成的复杂系统，而是一个本地 Flask 单体应用。
- `启动 Demo.bat` 进入 `workbench/` 并执行 `app.py`。
- `app.py` 负责接收页面操作和调度，具体能力主要放在 `core/`。
- `config/` 保存业务规则，`templates/` 决定页面和成果长什么样，`data/` 保存运行数据与输出。
- 当前提取方式默认是离线规则，而非在线大模型。

### 我仍然不理解什么？

- Flask 的“路由”具体如何把页面操作交给 Python 函数。
- `core/` 各模块在完整业务流程中的实际调用顺序。
- 页面模板、字段配置、数据库和生成文件在一次业务操作中如何串联。
- 44 个现有字段与真实 84 个收资项应如何取舍、映射和重构。

### 下一阶段重点

- 通过简短问答巩固模块 1，并将其标记为已完成。
- 进入模块 2，逐句阅读启动脚本并追踪到 Flask 服务可访问。
