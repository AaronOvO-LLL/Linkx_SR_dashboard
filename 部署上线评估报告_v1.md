# 灵石解决方案工作台 · 部署上线评估报告 v1

> 评估时间：2026-09-20
> 评估对象：`D:\claude-workspace\LinkX-SR-dashboard\灵石解决方案工作台\`
> 评估结论：**以「公网 / 生产环境上线」标准衡量 —— 不满足；以「内网 / 局域网演示上线」标准衡量 —— 基本满足，但需先完成 5 处必改项。**

---

## 一、结论速览

| 上线目标 | 是否满足 | 说明 |
|---|---|---|
| 本机演示（当前状态） | ✅ 满足 | 双击 `启动 Demo.bat`，浏览器访问 `127.0.0.1:8770` |
| 内网 / 局域网访问 | ⚠️ 差 5 处必改项 | 约 0.5 天可完成 |
| 公网 / 生产环境 | ❌ 不满足 | 缺少生产 WSGI、HTTPS、进程守护、部署产物 |

**一句话总结**：这是一个结构良好、测试完备的**单体 Flask 演示应用**，但它的定位是"本机运行的工具"，不是"面向外网的站点"。功能没问题，缺的是**部署工程化**和**上线安全加固**。

---

## 二、项目是什么

| 维度 | 现状 |
|---|---|
| 类型 | 模块化单体：Flask + Jinja2 + 原生 HTML/CSS/JS |
| 入口 | `workbench/app.py`（25 个路由，7 步业务流程） |
| 数据 | SQLite（WAL 模式），`workbench/data/workbench.db` |
| 启动 | Windows `启动 Demo.bat` → `python app.py` → 端口 8770 |
| 外部依赖 | DeepSeek（结构化提取）、腾讯云 ASR + COS（录音转写） |
| 交付物 | 踏勘结果 / 价值卡片 / 交付内容 / 提资清单（MD + HTML + CSV + ZIP） |

### 自检结果（本次实测）

| 检查项 | 命令 | 结果 |
|---|---|---|
| 配置校验 | `python check_config.py --strict` | ✅ 通过（safety_butler v0.4.0，49 字段 / 34 必填，0 错误 0 警告） |
| 单元测试 | `python -m unittest discover` | ✅ 16 / 16 通过 |
| 主流程自测 | `python selftest.py` | ✅ 73 / 73 通过 |

> 功能健康度**高**。问题全部集中在"部署与安全"，不在"业务逻辑"。

---

## 三、P0 阻断项（上线前必须修复）

### 1. 服务只绑定本机回环地址
- **证据**：`workbench/config/app.json` → `"server": { "host": "127.0.0.1" }`
- **后果**：只有本机能访问，同一局域网/其它机器的浏览器打不开。
- **修复**：改为 `"0.0.0.0"`，并用系统防火墙放行端口。

### 2. 使用 Flask 开发服务器对外服务
- **证据**：`app.py:751` → `app.run(host=host, port=port, debug=False, threaded=True)`；运行日志明确输出
  `WARNING: This is a development server. Do not use it in a production deployment.`
- **后果**：开发服务器无并发保护、无超时/进程管理，不适合承载真实访问，存在被拖垮风险。
- **修复**：改用生产 WSGI（Windows 推荐 **Waitress**，Linux 推荐 **Gunicorn/uWSGI**），并在其前面放 Nginx/Caddy 反向代理。
- **注意**：`requirements.txt` 当前**未包含**任何生产 WSGI 服务器。

### 3. 会话密钥硬编码在明文配置里
- **证据**：`app.json` → `"session": { "secret_key": "lingshi-demo-secret-change-me" }`
- **后果**：密钥一旦泄露，攻击者可伪造任意分部的登录会话（越权访问全部项目数据）。
- **修复**：改为从环境变量读取（如 `LS_SESSION_SECRET`），启动时若未设置则**拒绝启动**，不要保留可用默认值。

### 4. 全部 5 个分部公用默认口令 `000000`
- **证据**：`workbench/config/departments.json` → 5 个部门 `"password": "000000"`, `"changed": false`
- **后果**：等同于无认证；任何人可进入任一分部。
- **修复**：上线前逐一分部改密（应用内已有「修改密码」页），并删除配置中的默认口令回退。

### 5. 无 HTTPS，密码/录音走明文传输
- **后果**：登录口令、上传的原始录音在网络中明文可见。
- **修复**：由反向代理终止 TLS，申请证书（Let's Encrypt / 企业证书），HTTP 强制 301 跳转 HTTPS；Session Cookie 加 `Secure` + `HttpOnly` + `SameSite=Lax`。

---

## 四、P1 稳定性 / 运维项（强烈建议同期处理）

| # | 问题 | 证据 / 位置 | 影响 | 建议 |
|---|---|---|---|---|
| 6 | SQLite 单机单写 | `data/workbench.db` + WAL | 多进程/多 worker 写并发受限，无法水平扩展 | 明确"单实例部署"约束；或迁移 PostgreSQL |
| 7 | ASR 走进程内 daemon 线程 | `services/audio.py:183` `threading.Thread(daemon=True)` | 多 worker 部署时任务只在收到请求的进程里跑；进程重启可能中断在途任务 | 生产改为独立 worker 进程 / 队列；或锁定单进程部署 |
| 8 | 启动自动打开浏览器 | `app.json` `"open_browser": true` | 服务器上是无意义行为 | 服务器环境置为 `false` |
| 9 | `open_browser` 之外无日志落盘策略 | `data_server.log` 仅开发服务器输出 | 出问题难追溯 | 接入日志文件 + 轮转（Waitress 日志 / Nginx access log） |
| 10 | 无部署产物 | 无 Dockerfile / Procfile / wsgi.py / systemd / nginx.conf | 无法一键/可复现部署 | 补 `wsgi.py` + 部署脚本 + 反代配置 |
| 11 | 依赖未锁版本 | `requirements.txt`：`flask>=3.0` | 复现性差，未来安装可能踩新版本坑 | 锁定精确版本或 `pip freeze` 基线 |
| 12 | 无项目级 README | 根目录无部署说明（仅 `workbench/ARCHITECTURE.md`） | 交付/交接成本高 | 补一份「部署与运维说明」 |

---

## 五、P2 安全 / 合规 / 卫生

- **真实录音留在磁盘**：`record-test/` 内有 `通天地踏勘1.m4a`（约 33MB）等真实客户录音。已在 `.gitignore` 忽略（**未进代码库，做得对**），但作为敏感数据，建议评估留存必要性、加密存放或定期清理。
- **原始 Excel 未入库**：`资料输入区/踏勘要求的项目具体内容.xlsx` 同样被 `.gitignore` 忽略（正确）。
- **`.env.example` 缺失**：`.gitignore` 中写了 `!.env.example` 例外，但仓库里没有该文件，新环境上手时不知道要配哪些变量。
- **外部依赖未配置时功能不可用**：`config/asr.json` `enabled: true`，但需环境变量 `TENCENTCLOUD_SECRET_ID / SECRET_KEY / TENCENT_COS_BUCKET / REGION / PREFIX`；`config/app.json` 提取链路需 `DEEPSEEK_API_KEY`。未配置时页面会显示"未配置"，录音/大模型功能不可用（行为安全，但上线前须确认已配齐）。
- **运行数据会持续膨胀**：`data/outputs`、`data/uploads`、`data/exports` 无清理策略，建议加保留期/定期归档。
- **仓库所有权异常**：`git` 报 `dubious ownership`（`CRAZY-RYZEN/CodexSandboxOffline` vs 当前 `Aaron`）。属本机沙箱环境问题，非项目缺陷，但会阻碍提交，需 `git config --global --add safe.directory ...` 修复。

---

## 六、已经做得好的地方（保持）

- ✅ `.gitignore` 覆盖密钥、数据库、日志、真实录音与原始 Excel，敏感文件未误入库。
- ✅ 真实密钥全部走**环境变量**，未硬编码进 JSON / 代码 / 日志。
- ✅ 分层清晰（`app.py` / `services/` / `core/` / `providers/` / `config/`），有明确架构约束文档。
- ✅ 有数据库**迁移机制**、**配置校验器**、**单元测试**与**端到端自测**，主流程受保护。
- ✅ 有权限与租户边界检查（项目/产品访问校验 `dept_key`）。
- ✅ 已有 `data/backup/` 备份目录。

---

## 七、两条上线路径

### 路径 A：内网演示上线（推荐先做，成本最低）
1. `app.json`：`server.host` 改 `0.0.0.0`；`open_browser` 改 `false`。
2. `session.secret_key` 改为从环境变量读取。
3. 逐分部修改默认密码 `000000`。
4. 用 Waitress 承载（`pip install waitress` → `waitress-serve --host=0.0.0.0 --port=8770 app:app`）。
5. 系统防火墙放行 8770，用内网 IP 访问。
6. 配齐 `DEEPSEEK_API_KEY`、腾讯云 ASR/COS 环境变量。
> 预计改动集中在 4 个文件 + 1 个启动脚本，约半天。

### 路径 B：公网生产上线（在 A 之上追加）
7. Nginx/Caddy 反向代理 + HTTPS（强制跳转）。
8. 进程守护（Windows 服务 / NSSM，或 Linux systemd），崩溃自拉起。
9. Session Cookie 加固（`Secure` / `HttpOnly` / `SameSite`）。
10. ASR 线程模型与多 worker 冲突处理（改独立 worker 或锁定单进程）。
11. 数据库与生成物定期备份 + 日志轮转。
12. 锁依赖版本，补部署文档与回滚步骤。

---

## 八、上线前检查清单

- [ ] `server.host` 已改为 `0.0.0.0`（或反代地址）
- [ ] `secret_key` 来自环境变量，无可用默认值
- [ ] 5 个分部默认口令已全部更换
- [ ] 已启用 HTTPS，HTTP 强制跳转
- [ ] Session Cookie 已加 `Secure` / `HttpOnly` / `SameSite`
- [ ] 生产 WSGI（Waitress/Gunicorn）+ 进程守护已就位
- [ ] `DEEPSEEK_API_KEY`、腾讯云 ASR/COS 环境变量已配齐并验证
- [ ] `python check_config.py --strict` 通过
- [ ] `python -m unittest discover` 通过
- [ ] `python selftest.py` 通过
- [ ] 数据库与生成物备份策略已建立
- [ ] 真实录音等敏感数据留存已评估
