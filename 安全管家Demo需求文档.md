# 安全管家踏勘与方案生成 Demo 需求文档

> 文档状态：讨论稿 v0.3  
> 目标读者：产品负责人、设计 Agent、开发 Agent、测试 Agent  
> 更新时间：2026-09-02

## 0. 项目协作与审核机制

### 0.1 角色定位

本项目采用“需求负责人决策、审核 Agent 把关、执行 Agent 交付”的协作方式。

| 角色 | 主要职责 | 不承担的职责 |
|---|---|---|
| 需求负责人（用户） | 提供业务资料，确认业务规则，决定范围、优先级和重大取舍 | 不要求亲自完成设计和开发 |
| 总审核 Agent（当前对话中的 AI） | 维护需求基线、提出关键问题、拆解并明确任务、审核交付物、记录问题、判断是否通过 | 原则上不直接替代执行 Agent 完成页面设计、开发或测试任务 |
| 产品/业务 Agent | 从业务资料形成字段模型、业务规则、流程和验收标准 | 不擅自改变已确认的业务边界 |
| UX/UI Agent | 完成信息架构、页面流程、交互状态和视觉方案 | 不擅自删减功能或修改业务规则 |
| 技术方案 Agent | 完成架构、数据模型、接口、AI 流程、存储和部署设计 | 不擅自扩大产品范围 |
| 开发 Agent | 按已审核方案实现页面和功能，并提交自测证据 | 不以技术便利为由静默改变需求 |
| 测试 Agent | 根据需求和验收标准设计、执行测试，报告缺陷和风险 | 不替代需求负责人决定业务取舍 |

### 0.2 总审核 Agent 的工作方式

总审核 Agent 后续主要执行以下工作：

1. 将需求负责人的讨论结论更新为清晰、可追溯的需求基线。
2. 在开始执行前，为其他 Agent 明确输入材料、任务范围、输出格式和验收标准。
3. 审核其他 Agent 的交付是否完整、一致、可实现、可测试，并符合模块化要求。
4. 对发现的问题按严重程度给出“阻塞、重要、建议”三级结论。
5. 要求责任 Agent 修改并重新提交，而不是由总审核 Agent 默默代做。
6. 只有在交付满足验收标准后，才将对应阶段标记为通过。
7. 涉及业务口径、范围扩大或重大取舍时，提交需求负责人决定，不自行假设。

### 0.3 审核状态

每个阶段的交付物使用以下状态：

| 状态 | 含义 |
|---|---|
| 待开始 | 输入条件尚未满足或尚未分派 |
| 执行中 | 责任 Agent 正在完成任务 |
| 待审核 | 已提交交付物，等待总审核 Agent 检查 |
| 修改中 | 审核未通过，正在根据问题清单修改 |
| 已通过 | 已满足当前阶段验收标准 |
| 已搁置 | 经需求负责人确认，本阶段暂不继续 |

### 0.4 审核输出格式

总审核 Agent 对每份交付物至少给出：

- 审核结论：通过、有条件通过或不通过。
- 与需求基线的一致性检查。
- 问题清单及严重程度。
- 每个问题对应的修改要求和验收方法。
- 尚未解决的风险、假设与待决策事项。
- 下一责任 Agent、下一项任务及其输入条件。

### 0.5 需求基线原则

- 本文档是后续 Agent 工作的需求基线。
- 已确认需求与待确认假设必须明确分开。
- 任何 Agent 发现冲突或信息不足时，应提出问题，不得自行补写为已确认需求。
- 需求变更由需求负责人确认，总审核 Agent 负责记录并同步到相关任务。
- 执行 Agent 的方案和代码不能反向覆盖需求文档；确需调整时先走需求确认。

## 1. 文档目的

本文件用于定义“安全管家”首个可演示版本的产品需求，并作为后续 Agent 进行交互设计、技术设计、开发和测试的统一输入。

文档需要同时满足两个目标：

1. 首个版本可以完整演示一次真实业务闭环。
2. 数据、表单、产品和生成物均可配置，为后续新增产品、字段、模板和工作流预留扩展能力。

## 2. 产品目标

将现场调研产生的录音文字稿转化为结构化踏勘信息，提醒填写者补齐必要内容，并根据用户选择自动生成价值说明、费用估算、交付内容和提资资料，最终打包为 ZIP 下载。

### 2.1 Demo 核心价值

- 减少调研信息的重复整理和手工录入。
- 降低踏勘表漏填、错填和口径不统一的概率。
- 将调研结果快速转化为可沟通、可估算、可交付的标准材料。
- 验证“产品模板 + AI 提取 + 人工确认 + 文档生成”的可扩展工作模式。

### 2.2 首个版本成功标准

用户可以在一个连续流程中：

1. 创建或选择项目。
2. 在项目内选择“安全管家”产品。
3. 输入录音文字稿。
4. 由 AI 自动提取并填写踏勘内容。
5. 查看来源和待补信息，完成人工补充与确认。
6. 预览完整结果并勾选需要生成的材料。
7. 生成所选材料并下载一个 ZIP 文件。

## 3. 用户与使用场景

### 3.1 用户角色

| 角色 | 主要任务 | 首版是否需要独立权限 |
|---|---|---|
| 踏勘及方案人员 | 由同一人完成建项、调研录入、信息补齐、结果审核和材料生成 | 首版不拆分权限 |
| 管理员/产品配置人员 | 配置产品、字段、模板、规则 | 首版可不提供可视化后台，但底层必须可配置 |

首版不要求个人账号登录。访问者先选择所属分部并输入分部密码，再访问该分部的项目库。

### 3.2 组织与访问范围

- 首版提供一部、二部、三部、四部、五部五个分部入口。
- 每个分部使用独立访问密码。
- 初始默认密码均为 `000000`。
- 密码验证通过后，仅进入所选分部的项目库。
- 首版需要提供修改分部密码的基础方式；具体入口待交互设计时确定。
- 分部访问状态在当前使用会话中保持，退出分部后再次进入需要重新验证。
- 首版属于业务隔离和演示级访问控制，不视为正式的个人身份、权限和审计体系。
- 默认密码只适用于初始部署和 Demo；系统应明确提醒修改，未来多人版本不得继续使用统一弱密码。

### 3.3 核心演示故事

一名使用者为某客户创建项目，选择“安全管家”，粘贴现场访谈的录音文字稿。系统识别客户现状、问题、设备/点位、服务诉求和约束条件，并填入结构化踏勘表。系统将未识别、低置信度或业务规定必填的字段集中呈现，使用者补充并确认。随后使用者预览结果，勾选所需产物，系统生成文件并打包下载。

## 4. 产品范围

### 4.1 首版包含

- 项目的创建、列表和选择。
- 项目分类：内部项目、外部项目、重点项目。
- 项目复制、归档和删除。
- 项目内支持选择多个产品；首个实现和演示产品为“安全管家”。
- 录音文字稿的粘贴或文本录入。
- AI 提取结构化踏勘字段。
- 字段级状态展示：已提取、待确认、必须补填。
- 用户编辑、补充和确认。
- 踏勘结果预览。
- 生成物多选。
- 价值卡片、费用估算卡片、交付内容、提资基础内容的生成。
- 单项文件预览/查看与 ZIP 打包下载。
- 按分部保存项目库和项目进度，允许重新进入项目继续处理。
- 项目数据原则上永久保存，除非用户主动删除。

### 4.2 首版暂不默认包含

- 直接录音、实时转写或音频文件转写；首版只允许粘贴或输入文字稿。
- 多人实时协作、个人账号、复杂权限和审批流。
- 与 CRM、报价、合同、工单等外部系统集成。
- 完整的产品/表单/文档模板可视化配置后台。
- 正式商用级计费、审计和大规模并发保障。

## 5. 核心业务流程

### 5.1 主流程

1. 用户选择一部至五部中的一个分部，并输入该分部密码。
2. 密码验证通过后进入该分部项目库。
3. 创建新项目，或选择已有项目。
4. 选择项目分类并填写项目基础信息。
5. 在项目中新增或选择一个或多个调研产品。
6. 进入“安全管家”产品任务。
7. 输入录音文字稿并提交解析。
8. 系统提取踏勘信息并映射到产品字段。
9. 用户查看提取结果、原文依据和系统提示。
10. 用户补齐必填项，并确认低置信度内容。
11. 系统校验数据完整性。
12. 用户预览结构化踏勘结果。
13. 用户勾选需要生成的材料。
14. 系统生成所选内容。
15. 用户查看生成结果并下载 ZIP。

### 5.2 建议页面/步骤

| 步骤 | 页面 | 核心操作 |
|---|---|---|
| 1 | 分部入口 | 选择一部至五部，输入分部密码 |
| 2 | 项目中心 | 创建、搜索、选择、复制、归档或删除项目，查看项目状态 |
| 3 | 项目概览 | 编辑基础信息，选择多个产品，查看各产品处理进度 |
| 4 | 调研输入 | 粘贴文字稿，查看输入提示，启动 AI 解析 |
| 5 | 信息核对 | 查看自动填充内容、依据和置信提示，补齐必填项 |
| 6 | 预览与生成 | 预览踏勘结果，勾选生成物，触发生成 |
| 7 | 结果中心 | 查看单项结果、重新生成、下载 ZIP |

## 6. 功能需求

### 6.1 项目管理

#### 6.1.1 创建项目

首版建议至少包含以下项目字段：

- 所属分部（系统按当前项目库写入，必填）
- 项目分类：内部项目、外部项目、重点项目（必填，单选）
- 项目名称（必填）
- 客户名称（必填）
- 项目地点（可选或待确认）
- 项目负责人（可选或待确认）
- 联系人及联系方式（可选或待确认）
- 创建时间（系统生成）
- 最近更新时间（系统生成）
- 项目状态（系统维护）

#### 6.1.2 选择项目

- 展示已有项目列表。
- 支持按项目名称或客户名称查找。
- 展示最近更新时间、已选产品和当前进度。
- 进入项目后恢复上次保存的数据和步骤。

#### 6.1.3 项目状态

建议状态：草稿、调研处理中、待补充、待生成、已完成。

#### 6.1.4 项目复制、归档与删除

- 复制项目时复制项目基础信息、已选产品及对应结构化内容，生成新的项目标识和创建时间。
- 复制后的项目名称应明确标识为副本，并允许立即修改。
- 归档项目不再出现在默认项目列表中，但数据完整保留，可在归档列表中查看和恢复。
- 删除项目需要二次确认，并清晰说明影响范围。
- 为避免误操作，首版建议采用可恢复的软删除；是否提供回收站页面待交互设计时确认。
- 项目不要求保存每次编辑的完整历史版本，只保留当前有效内容及必要的生成结果记录。

### 6.2 产品选择

- 一个项目可以包含一个或多个产品实例；首版演示可只开放“安全管家”。
- 产品体系规划包含安全、消防、能源、设备四大产品；首版完整实现“安全”，其余产品可先展示为规划中或仅保留配置入口。
- 产品卡片展示产品名称、简述、所需信息范围和当前状态。
- 产品应由稳定的 `product_type` 与版本号标识，不能把业务逻辑写死在页面中。
- 同一产品模板升级后，历史项目继续保留创建时所用的模板版本。

### 6.3 调研文字稿输入

- 提供大段文本输入区域。
- 支持粘贴和手工编辑。
- 展示建议输入内容和示例结构。
- 提交解析前校验文本非空。
- 保留原始文字稿，后续提取和生成不得覆盖原文。
- 再次解析时提示其对现有人工编辑内容的影响，并优先保护已确认字段。

### 6.4 AI 自动提取与填写

- AI 按当前产品模板定义的字段，从文字稿中提取结构化内容。
- 每个自动提取字段至少记录：值、来源片段、状态、置信等级、提取时间、模板版本。
- 对无法确定的信息不得编造，应留空并标记为待补。
- 对存在多个可能答案或语义冲突的字段，标记为待确认。
- 可从同一段文字中提取多个结构化条目，例如点位清单、设备清单或风险项。
- AI 返回结果必须经过结构校验；失败时允许重试，并保留原始输入。

### 6.5 人工补充与确认

字段分为以下状态：

| 状态 | 含义 | 页面行为 |
|---|---|---|
| 已提取 | AI 已给出较明确内容 | 正常展示，可编辑 |
| 待确认 | 内容存在歧义、冲突或置信度较低 | 高亮并要求确认或修改 |
| 必须补填 | 必填但文字稿中未找到 | 集中展示并阻止最终生成 |
| 可选缺失 | 非必填且未找到 | 允许留空 |
| 已人工确认 | 用户确认或修改过 | 后续重新解析时默认保护 |

交互要求：

- 必填未完成数量持续可见。
- 支持按“全部、待确认、必须补填”筛选。
- 编辑字段后自动保存，或提供清晰的保存反馈。
- 展示 AI 填写依据；找不到依据时明确说明。
- 区分 AI 生成值与人工修改值。
- 对费用、数量等关键字段提供格式与范围校验。

### 6.6 完整性校验与预览

- 在进入生成步骤前执行必填、格式、业务规则和关联关系校验。
- 校验失败时准确定位字段，并提供修正入口。
- 预览页按业务章节展示，不只展示底层字段列表。
- 预览页明确显示将用于生成的最终数据。
- 用户确认预览后进入生成物选择。

### 6.7 生成物选择

首版候选生成物：

| 生成物 | 主要内容 | 初步格式 |
|---|---|---|
| 价值卡片 | 客户现状、核心问题、方案价值、预期收益、适用场景 | 待确认 |
| 费用估算卡片 | 估算项、数量、单价/计价规则、区间、小计、说明、假设条件 | 待确认 |
| 交付内容 | 交付范围、服务项、设备/系统、实施步骤、边界和验收说明 | 待确认 |
| 提资基础内容 | 需客户提供的信息、资料、接口、现场条件和责任人 | 待确认 |
| 完整踏勘结果 | 经确认的结构化调研内容 | 待确认 |

- 用户可多选生成物。
- 页面展示每项生成物的用途和主要包含内容。
- 至少选择一项后才能开始生成。
- 生成物定义来自配置，不在页面中写死。

### 6.8 内容生成与下载

- 按用户勾选内容逐项生成，并展示每项状态：等待、生成中、成功、失败。
- 单项失败不应导致已成功内容丢失。
- 允许针对失败项重试。
- 每项产物可单独查看或下载。
- 全部完成后，将成功产物与清单文件打包为 ZIP。
- ZIP 文件名建议包含项目、产品和生成时间。
- ZIP 内使用清晰、稳定的文件名和目录结构。
- 生成结果记录所用的输入版本、模板版本和生成时间，保证可追溯。

建议 ZIP 结构：

```text
项目名称_安全管家_时间.zip
├─ 01_踏勘结果/
├─ 02_价值卡片/
├─ 03_费用估算/
├─ 04_交付内容/
├─ 05_提资清单/
└─ manifest.json
```

## 7. 安全管家踏勘信息模型（待业务确认）

以下仅为字段分组骨架，需结合实际踏勘表、报价规则和交付模板补充。

### 7.1 项目与客户信息

- 客户/组织名称
- 场所名称与地址
- 联系人及联系方式
- 业务类型与场所类型
- 调研时间与调研人员

### 7.2 现状与目标

- 当前安全管理模式
- 当前人员与岗位安排
- 现有系统、设备和服务商
- 主要问题/风险
- 客户期望与优先级
- 期望上线时间

### 7.3 场所与点位

- 区域/楼栋/楼层
- 重点点位
- 出入口及周界
- 面积、数量、营业/使用时段
- 网络、供电、施工等现场条件

### 7.4 设备与系统

- 设备类型、品牌、型号、数量
- 设备状态与可复用性
- 接口/协议/联网条件
- 监控、门禁、报警、消防等系统情况
- 数据接入和权限条件

### 7.5 服务需求

- 巡检、值守、响应、报告等服务项
- 服务频次和时间窗口
- 告警处置与升级机制
- 驻场或远程要求
- 服务范围和不包含范围

### 7.6 商务与交付约束

- 预算或预算区间
- 计费周期
- 采购与审批要求
- 实施窗口
- 验收要求
- 特殊合规、安全或保密要求

### 7.7 提资需求

- 平面图、点位图、设备清单
- 网络拓扑、接口资料、账号权限
- 现行制度、应急预案、历史事件数据
- 联系人、现场配合人和责任分工
- 其他生成方案所需材料

## 8. 费用估算原则（待确认）

- 费用估算必须基于明确的计价项和规则，不允许模型自由编造单价。
- 可由 AI 判断适用的计价项和数量，但单价应来自受控价格配置，或由用户补填。
- 缺少关键计价依据时，输出区间、假设条件或“待确认”，不能给出虚假精确值。
- 费用结果需要区分一次性费用、周期性费用和可选费用。
- 所有金额需显示币种、含税口径和有效期（如适用）。

## 9. 模块化与可扩展设计要求

### 9.1 核心模块

| 模块 | 职责 | 扩展方向 |
|---|---|---|
| 项目模块 | 项目元数据、状态和进度 | 客户、组织、协作、权限 |
| 产品目录模块 | 产品定义与版本 | 新增产品、产品组合、上下架 |
| 表单/字段模型模块 | 字段、分组、类型、必填和规则 | 条件字段、重复表格、跨字段校验 |
| 调研输入模块 | 保存原文和输入来源 | 音频、图片、附件、OCR |
| AI 提取模块 | 原文到结构化字段 | 不同模型、提示词和抽取策略 |
| 人工核对模块 | 补充、确认、追溯 | 审批、多人协作、变更对比 |
| 规则与估算模块 | 完整性、业务规则、费用计算 | 规则引擎、价格版本、区域差异 |
| 内容生成模块 | 按模板生成产物 | 新模板、新格式、品牌主题 |
| 文件打包模块 | 文件组织与 ZIP 下载 | 对象存储、分享、归档 |

### 9.2 配置化边界

以下内容应尽量通过配置或独立模板定义：

- 产品名称、说明与版本。
- 字段分组、字段类型、必填规则和校验规则。
- AI 提取说明、字段释义和示例。
- 生成物清单、生成所需字段和输出模板。
- 费用项、计价单位、单价和计算规则。
- ZIP 目录及文件命名规则。

### 9.3 版本与追溯

- 产品模板、字段模型、费用规则和生成模板均需要版本标识。
- 项目保存其引用的版本；模板升级不得静默改变历史结果。
- 每次 AI 解析和产物生成都形成可追溯的运行记录。
- 人工修改需要保留最新值；是否需要完整历史版本待确认。

## 10. 数据实体草案

- `Project`：项目。
- `ProjectProduct`：项目中选择的某个产品实例。
- `ProductDefinition`：产品定义及版本。
- `FieldDefinition`：产品字段定义。
- `SurveySource`：文字稿及未来的音频、图片、附件来源。
- `SurveyFieldValue`：字段值、来源、状态与人工确认信息。
- `ExtractionRun`：一次 AI 提取任务。
- `ValidationResult`：一次完整性/业务规则校验结果。
- `PricingRuleSet`：费用规则版本。
- `ArtifactDefinition`：生成物定义。
- `ArtifactRun`：一次生成任务与结果。
- `ExportPackage`：ZIP 导出记录。

## 11. 非功能需求

### 11.1 可用性

- 新用户无需培训即可理解当前步骤和下一步操作。
- AI 处理中显示明确进度，失败时给出可恢复操作。
- 页面刷新或意外退出后，已保存内容不丢失。

### 11.2 性能（Demo 建议值，待确认）

- 常规页面操作在 2 秒内获得可见反馈。
- AI 提取和内容生成可异步执行，页面持续显示状态。
- 单次文字稿长度上限、文件数量和 ZIP 大小需要明确。

### 11.3 安全与隐私

- 文字稿可能包含客户、人员、场所和系统敏感信息，应限制访问并安全存储。
- 前端和日志不得暴露模型密钥或系统凭据。
- 日志应避免记录完整敏感正文，或应做脱敏。
- 数据保留、删除和模型数据使用策略待确认。

### 11.4 可测试性

- AI 提取结果与界面状态解耦，允许使用固定样例进行测试。
- 关键业务规则应具备确定性测试。
- 生成物需要可基于固定输入重复验证。

## 12. 异常与边界情况

- 文字稿为空、过短、超长或内容与安全管家无关。
- 文字稿包含矛盾信息、多个场所或多个客户。
- 解析超时、返回结构错误或部分字段解析失败。
- 用户重新解析但已有人工确认内容。
- 必填信息始终缺失。
- 费用规则缺失或数量无法确定。
- 部分生成物失败。
- ZIP 生成失败或下载链接过期。
- 用户关闭页面后重新进入。

## 13. 验收场景草案

### 场景 A：完整主流程

给定一段包含充分信息的安全管家调研文字稿，用户能够创建项目、选择产品、完成解析和确认，勾选至少两种生成物，并成功下载包含对应文件的 ZIP。

### 场景 B：必填项缺失

当文字稿缺少必填信息时，系统明确展示必须补填项；在补齐前阻止最终生成，在补齐后允许继续。

### 场景 C：低置信度内容

当原文存在歧义时，系统标记对应字段为待确认，并展示原文依据；用户确认或修改后状态更新。

### 场景 D：费用依据不足

当缺少数量或价格规则时，系统不得生成无依据的精确金额，而应提示补充、输出区间或标记待确认。

### 场景 E：部分生成失败

当某一生成物失败时，其他成功结果仍可查看和下载，失败项可单独重试。

### 场景 F：重新进入

用户退出并重新进入已有项目后，原始文字稿、字段内容、确认状态和已生成结果仍可恢复。

## 14. Demo 样例数据与演示脚本（待制作）

为保证演示稳定，需要准备：

- 一个标准客户项目样例。
- 一份接近真实口语、包含冗余和歧义的文字稿。
- 一套安全管家字段模板。
- 一套受控费用规则。
- 四类核心生成物模板。
- 一个完整成功流程，以及一个缺少必填项的演示分支。

## 15. 后续 Agent 任务与审核关口

| 阶段 | 责任 Agent | 必需输入 | 主要交付物 | 审核通过条件 |
|---|---|---|---|---|
| 1. 业务建模 | 产品/业务 Agent | 本需求文档、投入的业务资料 | 字段字典、必填规则、业务流程、费用规则、生成物映射 | 业务资料均有对应关系，无重要字段和规则遗漏 |
| 2. 交互设计 | UX/UI Agent | 已通过的业务模型 | 信息架构、页面流程、关键页面、状态与异常交互 | 完整覆盖主流程、异常、分部访问和多产品场景 |
| 3. 技术设计 | 技术方案 Agent | 已通过的业务模型与交互设计 | 架构、数据模型、接口、AI 流程、文件生成、部署方案 | 可支持首版闭环，并具备产品、字段和生成物扩展能力 |
| 4. 开发实现 | 开发 Agent | 已通过的交互和技术方案 | 可运行页面、配置、代码、自测记录 | 主流程可运行，关键异常可恢复，无未经确认的需求偏移 |
| 5. 独立测试 | 测试 Agent | 需求基线、可运行版本 | 测试方案、执行记录、缺陷清单、验收结论 | P0/P1 验收场景通过，阻塞缺陷清零 |
| 6. Demo 验收 | 总审核 Agent与需求负责人 | 上述已通过交付物 | 总体验收报告、遗留问题、后续建议 | 需求负责人确认可用于演示 |

各阶段的具体工作由对应执行 Agent 完成。总审核 Agent负责组织输入、审核结果和推动返工，原则上不代替责任 Agent 产出其主要交付物。

## 16. 已确认决策

1. 踏勘人员与方案人员为同一人，由一名使用者完成全流程。
2. 项目分为内部项目、外部项目和重点项目。
3. Demo 首先在单机运行，未来演进为多人访问的网页。
4. 首版不设置个人账号登录，使用一部至五部的分部选择和分部密码访问项目库。
5. 五个分部的初始默认密码均为 `000000`。
6. 首版只接收粘贴或手工输入的录音文字稿，不处理音频。
7. 一个项目可以选择多个产品。
8. 产品体系包含安全、消防、能源、设备四大产品，首版先实现安全管家。
9. 项目支持复制、归档和删除，但不要求保存完整编辑历史版本。
10. 项目及生成结果原则上永久保存，用户主动删除除外。
11. 生成结果不需要在线编辑。
12. 生成物已有固定模板，费用估算可能已有价格表，待资料投入后确认细节。

## 17. 待确认决策清单

### P0：需结合投入资料确认

1. “安全管家”的正式业务定义、现有踏勘表、字段类型和必填规则是什么？
2. 各类生成物的固定模板分别包含哪些内容和约束？
3. 费用估算是否已有完整价格表和公式，缺失部分如何处理？
4. 输出文件分别需要 Word、Excel、PDF、图片还是其他格式？
5. 投入内容是否包含真实客户敏感数据，需要采用什么脱敏或访问措施？

### P1：进入交互和技术设计前确认

1. 重新解析文字稿时，如何合并已有人工确认值？
2. 项目复制时，是否复制原始文字稿和已有生成物？
3. 删除采用可恢复的回收站，还是立即永久删除？
4. 分部密码由谁修改，首版通过页面还是配置文件维护？
5. 同一项目中的多个产品是共用一份文字稿，还是每个产品分别录入文字稿？

## 18. 当前明确假设

- 首版是用于验证业务闭环的 Demo，不等同于正式生产系统。
- 首个完整实现的产品为“安全管家”，但底层需支持安全、消防、能源、设备及后续新增产品。
- 首版核心输入是录音转写后的文字，不支持实时转写或音频上传。
- AI 负责提取和起草，人对关键字段和最终内容负责确认。
- 费用不得由大模型自由编造，必须由规则、价格表或人工输入支撑。
- 生成物选择、模板和 ZIP 目录需要配置化。
- 首版单机部署仍按可迁移到多人网页的边界设计数据和模块。
