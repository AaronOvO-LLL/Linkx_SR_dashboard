# 录音导入 · 工作计划 v1

> 编制日期：2026-09-15
> 编制角色：总审核 Agent（本对话）
> 配套文档：《安全管家Demo_录音导入需求文档_v1.md》《安全管家Demo_录音导入_技术方案对比_v1.md》
> 工作模式：**Agent 主导执行，需求负责人在 4 个关键节点介入**

---

## 1. 总体节奏

```
K1 决策关口 ──→ P1 需求基线 ──→ P2 技术设计 ──→ P3 开发 ──→ P4 自测 ──→ K4 验收
    ▲                ▲                                          ▲
 需要你：           需要你：                                    需要你：
 选厂商/给密钥/     确认需求文件             K3 抽检转写质量
 给真实录音                                   ▲
                                          需要你：听/看转写质量
        └────────── 并行线：字段模型对齐（需要你给业务口径）──────────┘
```

**关键路径：K1 → P1 → P2 → P3（转写链路）→ P4 → K4**
并行线「字段模型对齐」**不阻塞**转写链路的技术开发，但**阻塞场景 H 的效果验收**。

---

## 2. 需求负责人的 4 个介入点

| 节点 | 时机 | 你需要做的事 | 为什么必须由你定 |
|---|---|---|---|
| **K1 决策关口** | 立即（阻塞全部） | ① 选定转写通道与厂商 ② 提供密钥 ③ 确认是否允许录音上公有云 ④ 确认是否放宽"唯一 pip 依赖 flask"约束 ⑤ **提供 1 份真实 2h 踏勘录音样本（可脱敏）** | 涉及数据合规与技术栈约束，Agent 无权假设（§0.5） |
| **K2 需求基线确认** | P1 完成后 | 审核《录音导入需求文档 v1》，特别是 §2 三条基线变更、§5.4 术语表、§11 待确认清单 | 需求变更须由需求负责人确认（§0.5） |
| **K3 转写质量抽检** | P3 转写链路打通后 | 用真实录音跑一次，**亲眼确认转写准确率与术语矫正效果是否可用** | 这是本方案最贵的假设，必须实测而非推演 |
| **K4 端到端验收** | P4 完成后 | 走一遍场景 G，判断"人工操作是否超过 30 分钟" | 决定本方案是否真正解决了问题 |

**除以上 4 点外，其余环节由 Agent 连续执行，不设中间确认。** 遇到需业务口径的冲突时，Agent 挂起该项并继续推进其他项，不阻塞整体。

---

## 3. 阶段任务分解

### P0 前置准备（责任：需求负责人 + Agent 协助）

| 任务 | 责任方 | 交付物 | 验收判据 |
|---|---|---|---|
| P0.1 核实对比文档 §5 的 5 项待核实事项（V-1 至 V-5） | 技术方案 Agent | 待核实清单闭环结论 | 每项有明确结论或标注"实测后确认" |
| P0.2 选定通道与厂商、申请密钥 | **需求负责人** | 密钥（环境变量方式交付，**不得写入文档或代码**） | 能成功调用一次测试接口 |
| P0.3 数据合规决策（是否允许上公有云） | **需求负责人** | 决策记录 | 明确二选一 |
| P0.4 提供真实 2h 录音样本 | **需求负责人** | 音频文件（可脱敏） | 文件可读、时长符合 |
| P0.5 依赖约束决策 | **需求负责人** | 决策记录 | 明确放宽或维持 |

> **⚠️ P0 不闭环，P2 之后的任务全部无法开始。** 特别是 P0.4：没有真实录音，转写质量、术语矫正、分段策略三项都无法验证。

### P1 需求基线更新

| 任务 | 责任方 | 交付物 | 验收判据 |
|---|---|---|---|
| P1.1 将本文档与需求文件合并入原需求文档 | 产品/业务 Agent | 《安全管家Demo需求文档》v0.4 | §4.2/§16/§18 三条变更留痕，新增场景 G–K 完整 |
| P1.2 拟定首批业务术语表初稿（≥15 词 + 纠错映射） | 产品/业务 Agent | `asr_terms.json` 初稿 | 术语源自真实收资项，非编造 |
| P1.3 明确 `SurveySource` 与新增实体的字段口径 | 产品/业务 Agent | 数据实体说明 | 命名风格与现有 7 张表一致 |

**验收判据：** K2 需求负责人确认通过。

### P2 技术设计

| 任务 | 责任方 | 交付物 | 验收判据 |
|---|---|---|---|
| P2.1 架构与模块划分（含 `core/asr.py` provider 接口设计） | 技术方案 Agent | 架构设计文档 | provider 接口可切换厂商，上层零感知 |
| P2.2 转写任务状态机与异步执行方案 | 技术方案 Agent | 状态机设计 + 调度方案 | 覆盖 §9 全部异常分支 |
| P2.3 数据模型变更（6 个新增/扩展实体） | 技术方案 Agent | 建表脚本设计 | 兼容现有数据库，可迁移 |
| P2.4 密钥与日志脱敏方案 | 技术方案 Agent | 安全设计说明 | 密钥不进代码/前端/日志 |
| P2.5 分段提炼策略设计（切段规则 + map-reduce 流程 + 提示词） | 技术方案 Agent | 提炼策略文档 | 明确 2–4 万字下的输出契约 |
| P2.6 交互设计（3 个新页面：上传、转写校对、纪要确认） | UX/UI Agent | 页面流程与关键页面设计 | 遵守 §5.5 的 15 分钟红线；沿用现有页面风格与自动保存机制 |

**验收判据：** 总审核 Agent 审核通过（对照需求文件逐条检查覆盖度）。

### P3 开发实现

按依赖顺序拆为 6 个可独立验证的工作包：

| 工作包 | 内容 | 依赖 | 交付物 |
|---|---|---|---|
| **A. 上传与任务层** | 上传入口、文件校验、任务创建、状态查询接口、前端进度 | P2.1–P2.3 | 可上传并创建任务的页面 |
| **B. 转写适配层** | `core/asr.py` provider 接口 + 至少一个 provider 实现（云端或本地） | A、P0.2 | 可实际调通转写并落库 |
| **C. 术语矫正层** | 热词表映射（提交时）+ 纠错映射（取回后）+ 命中日志 | B、P1.2 | 矫正命中可追溯 |
| **D. 分段提炼层** | 切段、逐段纪要、全局汇总、人工调整 | B、P2.5 | 2h 转写可产出结构化纪要 |
| **E. 校核页面** | 转写校对页（介入点1）、纪要确认页（介入点2） | C、D、P2.6 | 两个页面可用，自动保存生效 |
| **F. 配置与衔接** | `asr.json`、`asr_terms.json`、生成物配置、`source_type=audio` 与提取环节衔接 | B、P1.3 | 提取环节可消费纪要输入 |

**开发红线（继承项目约定）：**
- 一切业务知识进配置，代码零业务硬编码（§9）
- 不引入 Vue / React，保持 Flask + Jinja2 + 原生 JS
- Windows 环境注意事项（`.bat` 编码、中文路径）沿用既有经验
- **不得以技术便利为由静默改变需求**（§0.1）

**验收判据：** 各工作包可独立演示；K3 需求负责人抽检转写质量。

### P4 自测与独立测试

| 任务 | 责任方 | 交付物 | 验收判据 |
|---|---|---|---|
| P4.1 扩展 `selftest.py`：新增场景 G–K | 开发 Agent | 自测脚本 | provider 使用 mock，无需真实云端即可跑全链路 |
| P4.2 用真实 2h 录音跑端到端 | 开发 Agent | 执行记录 | 场景 G/H/K 通过 |
| P4.3 异常分支测试（§9 全部 10 类） | 测试 Agent | 缺陷清单 | P0/P1 缺陷清零 |
| P4.4 回归测试：原 77 项自测仍全部通过 | 测试 Agent | 回归记录 | 无回归缺陷（**双输入源并存不得破坏原路径**） |

**验收判据：** P0/P1 验收场景通过，阻塞缺陷清零。

### P5 Demo 验收

| 任务 | 责任方 | 交付物 |
|---|---|---|
| P5.1 总体验收报告与遗留问题清单 | 总审核 Agent | 验收报告 |
| P5.2 判断"人工操作是否 ≤ 30 分钟" | **需求负责人** | 验收结论 |
| P5.3 同步更新 `项目阅读记录.md` 与 `安全管家Demo_阶段审核报告` | 总审核 Agent | 更新后的文档 |

---

## 4. 并行线：字段模型对齐（不阻塞开发，阻塞效果验收）

**问题背景（2026-09-15 修订）：** 阶段审核报告 B-2 原述"Demo 44 个字段与真实 84 个收资项完全同名仅 1 个"。——**该结论已过时**：`fields.json` 已于 09-07 23:06 重写为 v0.2.0，**49 字段与专项收资表（`踏勘要求的项目具体内容.xlsx`）项目信息17+安防32 完全 1:1 对齐**，且 `template_version 0.2.0` 的更新时间**晚于**审核报告。详见《安全管家Demo_字段模型对照_v1.md》。

**仍然成立的真问题：** 权威口径未定（专项表 A vs 在线填报系统 B），且专项表缺两类字段——① **接入可行性**（账号密码/监控机房/监控网络/算法需求/点位图，决定方案能否落地）② **需求意向**（客户要买哪个产品）。这两类只在旧在线填报系统（84 项 / 112 槽位）里存在。

**为什么必须并行处理：** 录音链路的产出是"更完整、更结构化的输入"，但提取目标不变的话，**输入质量提升无法转化为业务价值**。场景 H（长文本提取有效性）的验收标准本身依赖字段模型是否正确。

| 任务 | 责任方 | 你的介入 |
|---|---|---|
| 完成字段来源与口径决策（D-1~D-4），并据此补齐缺失字段 | 产品/业务 Agent 拟定方案 | **需要你给业务口径**：以哪个来源为准、哪些收资项进首版 |
| 明确"安全管家只是 S4 专项踏勘下 1/5 分组"带来的产品体系调整 | 产品/业务 Agent | 需要你决策是否调整 §16-8 的产品体系定义 |
| 处理 B-1 费用单价虚构问题 | 产品/业务 Agent | 需要你提供真实受控价格表或确认继续标注待确认 |

> **建议：** 这条线的优先级不低于录音链路，且**必须与 K1 同步启动**——否则录音做完了，字段还是错的。

---

## 5. 风险清单

| 风险 | 等级 | 影响 | 缓解措施 |
|---|---|---|---|
| **无真实 2h 录音样本** | 🔴 高 | 转写质量、术语矫正、分段策略全部无法验证，开发只能盲做 | P0.4 列为阻塞项，在 K1 强制闭环 |
| **"转写后校文本"变成新的负担** | 🔴 高 | 若需逐字校对 2 万字，本方案只是把问题换了个形式 | §5.5 设 15 分钟红线；系统只呈现"待确认术语 + 低置信片段" |
| **对象存储中转被低估** | 🟡 中 | 实际工作量可能翻倍；引入第二套密钥 | 方案对比 §2 已显式建模；P0.1 优先核实免中转路径 |
| **长文本提取仍失败** | 🟡 中 | 分段提炼做完了，但规则引擎仍提不出字段 | P2.5 明确输出契约；P4.2 用真实录音实测场景 H |
| **依赖约束被打破引发连锁影响** | 🟡 中 | 启动脚本、部署方式、单机迁移边界均受影响 | P0.5 提前决策；本地方案仅在隐私硬约束下启用 |
| **字段模型不同步** | 🟡 中 | 录音链路价值无法转化 | 并行线 K1 同步启动（见 §4） |
| 转写结果被厂商侧清理 | 🟢 低 | 任务无法取回 | §5.2 要求立即落库 |
| 隐私/合规问题 | 🔴 高 | 客户敏感信息外流 | P0.3 决策前置；未决策前不得上传公有云 |

---

## 6. 明确不做的事（防止范围蔓延）

- ❌ 实时转写 / 边录边转（§4.2 禁令保留）
- ❌ 音频在线播放编辑器（除"校文本"环节必要的回听定位外）
- ❌ 多轨专业录音设备适配、声纹识别身份核验
- ❌ 与 CRM / 报价 / 工单系统集成（§4.2）
- ❌ 完整的价格表与字段模型重构（属并行线，不在本任务范围）
- ❌ 删除或替代现有"粘贴文字稿"路径（**必须完整保留**）

---

## 7. 下一步（等你的决策）

**K1 是唯一阻塞项。** 请就以下 5 件事给出结论，然后 Agent 即可连续推进到 K2：

1. 选哪条转写通道？（建议：云端为主通道 + 本地 FunASR 为隐私备通道；若录音含高敏感信息，直接选本地）
2. 是否允许录音上公有云？
3. 是否放宽"唯一 pip 依赖 flask"约束？
4. **能否提供 1 份真实 2h 踏勘录音样本（可脱敏）？** ← 最关键
5. 密钥如何交付？（建议：环境变量，不写入任何文档）
